

WILL SEE THE FOLLOWING: 
for one on one messaging, the user that will get the message will have the following data structure: {userIdOfRecipient: (the id of the user that will get the messages from the current user), conversationId: the id of the conversation, messages:[{for the current user: {text: the txt of the message, timeOfSend: the time that the message was sent} for the other user it will have the following data structure: {isByRecipient: true, text: the text of the message here, the time of send}}]}


CASE:
the user deletes all of the messages from a specific user (the user clears all messages in the messages array for a specific user), keep the conversationId for that user



GOAL: userDeleted is following the userA, delete userDeleted from the userA's followers